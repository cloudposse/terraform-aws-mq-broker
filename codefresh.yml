version: '1.0'

steps:
  test:
    title: Terraform module test
    image: cloudposse/build-harness:${{BUILD_HARNESS_VERSION}}
    working_directory: /
    commands:
    - make init
    - make terraform/lint
    - make terraform/validate
    - make readme/lint
